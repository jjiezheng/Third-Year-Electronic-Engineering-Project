#include "Line.h"
#include "Material.h"


namespace de
{
    namespace classes
    {

        Line::Line() : priority(0), Points(), Colours(), iterPoints(), iterColours()
        {

        }
    }
}
